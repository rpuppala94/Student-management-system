import { useMemo, useState } from 'react';
import { Archive, BookOpen, CalendarDays, FileText, Search, X } from 'lucide-react';
import { StatCard } from '../../components/student';
import { DocumentCard } from '../../components/student/document-card';
import { documentCategories, documentSemesters, mockDocuments, type DocumentCategory, type StudentDocument } from '../../data/student-documents';

const getDocumentTypeCount = (category: DocumentCategory) => mockDocuments.filter((document) => document.category === category).length;

const DocumentPreview = ({ document, onClose }: { document: StudentDocument; onClose: () => void }) => <div className="fixed inset-0 z-50 flex items-end justify-center bg-slate-950/35 p-3 sm:items-center sm:p-6" role="presentation" onClick={onClose}><section className="max-h-[90vh] w-full max-w-xl overflow-y-auto rounded-2xl border border-slate-200 bg-white p-5 shadow-2xl sm:p-6" role="dialog" aria-modal="true" aria-labelledby="document-preview-title" onClick={(event) => event.stopPropagation()}><div className="flex items-start justify-between gap-4 border-b border-slate-100 pb-4"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-indigo-600">Document preview</p><h2 id="document-preview-title" className="mt-1 text-xl font-extrabold text-slate-900">{document.title}</h2></div><button type="button" aria-label="Close document preview" onClick={onClose} className="rounded-lg p-2 text-slate-400 hover:bg-slate-50"><X className="h-5 w-5" /></button></div><div className="mt-5 grid gap-3 rounded-xl bg-slate-50 p-4 text-sm sm:grid-cols-2"><div><p className="text-xs font-semibold text-slate-400">Subject</p><p className="mt-1 font-bold text-slate-800">{document.subject}</p></div><div><p className="text-xs font-semibold text-slate-400">Category</p><p className="mt-1 font-bold text-slate-800">{document.category}</p></div><div><p className="text-xs font-semibold text-slate-400">Semester</p><p className="mt-1 font-bold text-slate-800">{document.semester}</p></div><div><p className="text-xs font-semibold text-slate-400">File</p><p className="mt-1 font-bold text-slate-800">{document.fileType} • {document.fileSize}</p></div><div><p className="text-xs font-semibold text-slate-400">Uploaded by</p><p className="mt-1 font-bold text-slate-800">{document.uploadedBy}</p></div><div><p className="text-xs font-semibold text-slate-400">Uploaded date</p><p className="mt-1 font-bold text-slate-800">{new Date(`${document.uploadedDate}T12:00:00`).toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' })}</p></div></div><div className="mt-5 grid min-h-48 place-items-center rounded-xl border border-dashed border-indigo-200 bg-indigo-50/40 p-6 text-center"><FileText className="h-10 w-10 text-indigo-300" /><p className="mt-3 text-sm font-bold text-slate-700">Preview placeholder</p><p className="mt-1 text-xs text-slate-500">A document viewer will be connected when file storage is available.</p></div></section></div>;

export const Documents = () => {
    const [search, setSearch] = useState('');
    const [category, setCategory] = useState<'All' | DocumentCategory>('All');
    const [subject, setSubject] = useState('All Subjects');
    const [semester, setSemester] = useState('All Semesters');
    const [sort, setSort] = useState('newest');
    const [selectedDocument, setSelectedDocument] = useState<StudentDocument>();
    const [feedback, setFeedback] = useState('');
    const subjects = ['All Subjects', ...Array.from(new Set(mockDocuments.map((document) => document.subject)))];
    const documents = useMemo(() => mockDocuments.filter((document) => { const query = search.trim().toLowerCase(); return [document.title, document.subject, document.category, document.uploadedBy].some((value) => value.toLowerCase().includes(query)) && (category === 'All' || document.category === category) && (subject === 'All Subjects' || document.subject === subject) && (semester === 'All Semesters' || document.semester === semester); }).sort((first, second) => sort === 'oldest' ? first.uploadedDate.localeCompare(second.uploadedDate) : sort === 'name' ? first.title.localeCompare(second.title) : second.uploadedDate.localeCompare(first.uploadedDate)), [search, category, subject, semester, sort]);
    const clearFilters = () => { setSearch(''); setCategory('All'); setSubject('All Subjects'); setSemester('All Semesters'); setSort('newest'); };
    const download = (document: StudentDocument) => { setFeedback(`Download started for ${document.title}.`); window.setTimeout(() => setFeedback(''), 3500); };

    return <main className="mx-auto max-w-[1500px] p-4 sm:p-6 lg:p-8"><section className="mb-6"><p className="text-xs font-bold uppercase tracking-[0.18em] text-indigo-600">Academic library</p><h1 className="mt-2 text-2xl font-extrabold tracking-tight text-slate-950 sm:text-3xl">Documents</h1><p className="mt-1.5 text-sm text-slate-500">Access your study materials, question papers, assignments, and academic documents.</p><p className="mt-2 text-sm font-medium text-slate-500">B.Tech CSE <span className="px-1 text-slate-300">•</span> 3rd Year <span className="px-1 text-slate-300">•</span> 5th Semester <span className="px-1 text-slate-300">•</span> Section A</p></section><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard title="Total Documents" value={String(mockDocuments.length)} description="Available to view" icon={FileText} accent="blue" /><StatCard title="Question Papers" value={String(getDocumentTypeCount('Question Papers'))} description="Past assessments" icon={BookOpen} accent="emerald" /><StatCard title="Study Materials" value={String(getDocumentTypeCount('Study Materials'))} description="Notes and resources" icon={Archive} accent="amber" /><StatCard title="Recent Uploads" value={String(mockDocuments.filter((document) => document.uploadedDate >= '2026-09-01').length)} description="Added this month" icon={CalendarDays} accent="rose" /></section><section className="mt-6 rounded-2xl border border-slate-200/80 bg-white p-4 shadow-sm sm:p-5"><div className="flex flex-col gap-3 lg:flex-row"><label className="relative block min-w-0 flex-1"><span className="sr-only">Search documents</span><Search className="pointer-events-none absolute left-3.5 top-3 h-4 w-4 text-slate-400" /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search documents..." className="w-full rounded-xl border border-slate-200 bg-slate-50 py-2.5 pl-10 pr-4 text-sm outline-none focus:border-indigo-500 focus:bg-white focus:ring-2 focus:ring-indigo-100" /></label><select aria-label="Filter by category" value={category} onChange={(event) => setCategory(event.target.value as 'All' | DocumentCategory)} className="rounded-xl border border-slate-200 bg-slate-50 px-3.5 py-2.5 text-sm font-semibold text-slate-600 outline-none focus:border-indigo-500 focus:ring-2 focus:ring-indigo-100">{documentCategories.map((option) => <option key={option}>{option}</option>)}</select><select aria-label="Filter by subject" value={subject} onChange={(event) => setSubject(event.target.value)} className="rounded-xl border border-slate-200 bg-slate-50 px-3.5 py-2.5 text-sm font-semibold text-slate-600 outline-none focus:border-indigo-500 focus:ring-2 focus:ring-indigo-100">{subjects.map((option) => <option key={option}>{option}</option>)}</select><select aria-label="Filter by semester" value={semester} onChange={(event) => setSemester(event.target.value)} className="rounded-xl border border-slate-200 bg-slate-50 px-3.5 py-2.5 text-sm font-semibold text-slate-600 outline-none focus:border-indigo-500 focus:ring-2 focus:ring-indigo-100">{documentSemesters.map((option) => <option key={option}>{option}</option>)}</select><select aria-label="Sort documents" value={sort} onChange={(event) => setSort(event.target.value)} className="rounded-xl border border-slate-200 bg-slate-50 px-3.5 py-2.5 text-sm font-semibold text-slate-600 outline-none focus:border-indigo-500 focus:ring-2 focus:ring-indigo-100"><option value="newest">Newest</option><option value="oldest">Oldest</option><option value="name">Name A-Z</option></select></div></section><section className="mt-6"><div className="mb-4 flex items-end justify-between"><div><h2 className="text-lg font-bold text-slate-900">Documents</h2><p className="mt-1 text-sm text-slate-500">{documents.length} of {mockDocuments.length} documents shown</p></div></div>{documents.length > 0 ? <div className="grid gap-4 lg:grid-cols-2">{documents.map((document) => <DocumentCard key={document.id} document={document} onView={() => setSelectedDocument(document)} onDownload={() => download(document)} />)}</div> : <div className="rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center"><FileText className="mx-auto h-7 w-7 text-slate-300" /><h2 className="mt-3 text-base font-bold text-slate-800">No documents found</h2><p className="mt-1 text-sm text-slate-500">Try changing your search or filters.</p><button type="button" onClick={clearFilters} className="mt-5 rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-bold text-white hover:bg-indigo-700">Clear Filters</button></div>}</section>{feedback && <div role="status" className="fixed bottom-5 right-5 z-40 rounded-xl bg-slate-900 px-4 py-3 text-sm font-semibold text-white shadow-lg">{feedback}</div>}{selectedDocument && <DocumentPreview document={selectedDocument} onClose={() => setSelectedDocument(undefined)} />}</main>;
};