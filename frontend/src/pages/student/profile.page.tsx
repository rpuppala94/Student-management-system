import { useState } from 'react';
import { AtSign, BookOpen, CalendarDays, CheckCircle2, Edit3, GraduationCap, Mail, MapPin, Phone, Save, ShieldCheck, User, Users, X } from 'lucide-react';
import { mockStudentProfile, type StudentProfile } from '../../data/student-profile';

type EditableContact = Pick<StudentProfile, 'phone' | 'personalEmail' | 'address'>;
type ContactErrors = Partial<Record<keyof EditableContact, string>>;

const InfoItem = ({ label, value, icon: Icon }: { label: string; value: string; icon?: typeof User }) => <div className="flex gap-3"><div className="mt-0.5 text-slate-400">{Icon ? <Icon className="h-4 w-4" /> : <span className="block h-4 w-4" />}</div><div className="min-w-0"><p className="text-xs font-semibold text-slate-400">{label}</p><p className="mt-1 break-words text-sm font-bold text-slate-800">{value}</p></div></div>;

export const Profile = () => {
    const [profile, setProfile] = useState(mockStudentProfile);
    const [draft, setDraft] = useState<EditableContact>({ phone: profile.phone, personalEmail: profile.personalEmail, address: profile.address });
    const [isEditing, setIsEditing] = useState(false);
    const [errors, setErrors] = useState<ContactErrors>({});
    const [feedback, setFeedback] = useState('');

    const startEditing = () => { setDraft({ phone: profile.phone, personalEmail: profile.personalEmail, address: profile.address }); setErrors({}); setIsEditing(true); };
    const cancelEditing = () => { setDraft({ phone: profile.phone, personalEmail: profile.personalEmail, address: profile.address }); setErrors({}); setIsEditing(false); };
    const saveChanges = () => {
        const nextErrors: ContactErrors = {};
        if (!/^\+91\s?\d{10}$/.test(draft.phone.replace(/-/g, ''))) nextErrors.phone = 'Enter a valid Indian phone number.';
        if (!/^\S+@\S+\.\S+$/.test(draft.personalEmail)) nextErrors.personalEmail = 'Enter a valid email address.';
        if (!draft.address.trim()) nextErrors.address = 'Address is required.';
        setErrors(nextErrors);
        if (Object.keys(nextErrors).length > 0) return;
        setProfile((current) => ({ ...current, ...draft }));
        setIsEditing(false);
        setFeedback('Profile updated successfully.');
        window.setTimeout(() => setFeedback(''), 3500);
    };
    const updateDraft = (field: keyof EditableContact, value: string) => setDraft((current) => ({ ...current, [field]: value }));

    return <main className="mx-auto max-w-[1500px] p-4 sm:p-6 lg:p-8"><section className="mb-6"><p className="text-xs font-bold uppercase tracking-[0.18em] text-indigo-600">Student account</p><h1 className="mt-2 text-2xl font-extrabold tracking-tight text-slate-950 sm:text-3xl">Profile</h1><p className="mt-1.5 text-sm text-slate-500">View and manage your personal and academic information.</p><p className="mt-2 text-sm font-medium text-slate-500">B.Tech CSE <span className="px-1 text-slate-300">•</span> 3rd Year <span className="px-1 text-slate-300">•</span> 5th Semester <span className="px-1 text-slate-300">•</span> Section A</p></section><section className="rounded-2xl border border-indigo-100 bg-indigo-50/60 p-5 shadow-sm sm:p-7"><div className="flex flex-col gap-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-4"><div className="grid h-20 w-20 shrink-0 place-items-center rounded-2xl bg-gradient-to-br from-indigo-600 to-blue-600 text-2xl font-extrabold text-white shadow-lg shadow-indigo-600/20">RK</div><div><div className="flex flex-wrap items-center gap-2"><h2 className="text-xl font-extrabold text-slate-900">{profile.fullName}</h2><span className="inline-flex items-center gap-1 rounded-full bg-emerald-100 px-2.5 py-1 text-xs font-bold text-emerald-700"><CheckCircle2 className="h-3.5 w-3.5" />Active Student</span></div><p className="mt-1 text-sm font-semibold text-indigo-700">{profile.id}</p><p className="mt-1 text-sm text-slate-600">B.Tech Computer Science and Engineering</p><p className="mt-1 text-xs text-slate-500">{profile.department} <span className="px-1 text-slate-300">•</span> {profile.year} <span className="px-1 text-slate-300">•</span> {profile.semester} <span className="px-1 text-slate-300">•</span> Section {profile.section}</p></div></div></div></section><div className="mt-6 grid gap-6 xl:grid-cols-[1.15fr_0.85fr]"><section className="rounded-2xl border border-slate-200/80 bg-white p-5 shadow-sm sm:p-6"><div className="flex items-center justify-between gap-3"><div className="flex items-center gap-3"><div className="rounded-xl bg-indigo-50 p-2 text-indigo-600"><User className="h-5 w-5" /></div><div><h2 className="font-bold text-slate-900">Personal Information</h2><p className="text-xs text-slate-500">Basic contact details</p></div></div>{!isEditing && <button type="button" onClick={startEditing} className="inline-flex items-center gap-1.5 rounded-xl border border-indigo-200 px-3 py-2 text-xs font-bold text-indigo-700 hover:bg-indigo-50"><Edit3 className="h-3.5 w-3.5" />Edit Profile</button>}</div>{isEditing ? <div className="mt-5 space-y-4"><InfoItem label="Full Name" value={profile.fullName} /><InfoItem label="Date of Birth" value={profile.dateOfBirth} /><InfoItem label="Gender" value={profile.gender} /><label className="block"><span className="text-xs font-semibold text-slate-400">Phone</span><input value={draft.phone} onChange={(event) => updateDraft('phone', event.target.value)} className="mt-1.5 w-full rounded-xl border border-slate-200 px-3.5 py-2.5 text-sm outline-none focus:border-indigo-500 focus:ring-2 focus:ring-indigo-100" />{errors.phone && <span className="mt-1 block text-xs font-bold text-rose-600">{errors.phone}</span>}</label><label className="block"><span className="text-xs font-semibold text-slate-400">Personal Email</span><input type="email" value={draft.personalEmail} onChange={(event) => updateDraft('personalEmail', event.target.value)} className="mt-1.5 w-full rounded-xl border border-slate-200 px-3.5 py-2.5 text-sm outline-none focus:border-indigo-500 focus:ring-2 focus:ring-indigo-100" />{errors.personalEmail && <span className="mt-1 block text-xs font-bold text-rose-600">{errors.personalEmail}</span>}</label><label className="block"><span className="text-xs font-semibold text-slate-400">Address</span><textarea rows={3} value={draft.address} onChange={(event) => updateDraft('address', event.target.value)} className="mt-1.5 w-full resize-none rounded-xl border border-slate-200 px-3.5 py-2.5 text-sm outline-none focus:border-indigo-500 focus:ring-2 focus:ring-indigo-100" />{errors.address && <span className="mt-1 block text-xs font-bold text-rose-600">{errors.address}</span>}</label><div className="flex flex-wrap gap-2"><button type="button" onClick={saveChanges} className="inline-flex items-center gap-2 rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-bold text-white hover:bg-indigo-700"><Save className="h-4 w-4" />Save Changes</button><button type="button" onClick={cancelEditing} className="inline-flex items-center gap-2 rounded-xl border border-slate-200 px-4 py-2.5 text-sm font-bold text-slate-600 hover:bg-slate-50"><X className="h-4 w-4" />Cancel</button></div></div> : <div className="mt-5 grid gap-5 sm:grid-cols-2"><InfoItem label="Full Name" value={profile.fullName} icon={User} /><InfoItem label="Date of Birth" value={profile.dateOfBirth} icon={CalendarDays} /><InfoItem label="Gender" value={profile.gender} icon={User} /><InfoItem label="Phone" value={profile.phone} icon={Phone} /><InfoItem label="Personal Email" value={profile.personalEmail} icon={Mail} /><InfoItem label="Address" value={profile.address} icon={MapPin} /></div>}</section><section className="rounded-2xl border border-slate-200/80 bg-white p-5 shadow-sm sm:p-6"><div className="flex items-center gap-3"><div className="rounded-xl bg-emerald-50 p-2 text-emerald-600"><GraduationCap className="h-5 w-5" /></div><div><h2 className="font-bold text-slate-900">Academic Information</h2><p className="text-xs text-slate-500">Read-only academic identity</p></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><InfoItem label="Student ID" value={profile.id} icon={AtSign} /><InfoItem label="Department" value={profile.department} icon={BookOpen} /><InfoItem label="Program" value={profile.program} icon={GraduationCap} /><InfoItem label="Batch" value={profile.batch} /><InfoItem label="Year" value={profile.year} /><InfoItem label="Semester" value={profile.semester} /><InfoItem label="Section" value={profile.section} /><InfoItem label="Admission Year" value={String(profile.admissionYear)} /><InfoItem label="Academic Status" value={profile.academicStatus} icon={ShieldCheck} /></div></section></div><div className="mt-6 grid gap-6 lg:grid-cols-2"><section className="rounded-2xl border border-slate-200/80 bg-white p-5 shadow-sm sm:p-6"><div className="flex items-center gap-3"><div className="rounded-xl bg-amber-50 p-2 text-amber-600"><Users className="h-5 w-5" /></div><div><h2 className="font-bold text-slate-900">Parent / Guardian Information</h2><p className="text-xs text-slate-500">Read-only emergency contact</p></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><InfoItem label="Guardian Name" value={profile.guardianName} icon={User} /><InfoItem label="Relationship" value={profile.guardianRelationship} /><InfoItem label="Phone" value={profile.guardianPhone} icon={Phone} /><InfoItem label="Email" value={profile.guardianEmail} icon={Mail} /></div></section><section className="rounded-2xl border border-slate-200/80 bg-white p-5 shadow-sm sm:p-6"><div className="flex items-center gap-3"><div className="rounded-xl bg-sky-50 p-2 text-sky-600"><ShieldCheck className="h-5 w-5" /></div><div><h2 className="font-bold text-slate-900">Account Information</h2><p className="text-xs text-slate-500">Read-only account profile</p></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><InfoItem label="Role" value={profile.role} /><InfoItem label="Username / Student ID" value={profile.id} /><InfoItem label="Login Email" value={profile.personalEmail} icon={AtSign} /><InfoItem label="Account Status" value={profile.accountStatus} icon={CheckCircle2} /></div></section></div>{feedback && <div role="status" className="fixed bottom-5 right-5 z-40 rounded-xl bg-slate-900 px-4 py-3 text-sm font-semibold text-white shadow-lg">{feedback}</div>}</main>;
};