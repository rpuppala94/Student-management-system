import { useState } from 'react';
import { Award, BookOpen, Calculator, CheckCircle2, GraduationCap, TrendingUp, Trophy, XCircle } from 'lucide-react';
import { StatCard } from '../../components/student';
import { ResultCard } from '../../components/student/result-card';
import { calculateCgpa, calculateSemesterSgpa, classRank, getCreditsEarned, getGradeFromMarks, getResultGradePoint, getResultStatus, previousSemesterHistory, resultSemesterOptions, resultsBySemester, type ResultSemester } from '../../data/student-results';

export const Results = () => {
    const [semester, setSemester] = useState<ResultSemester>('5th Semester');
    const results = resultsBySemester[semester];
    const semesterSgpa = calculateSemesterSgpa(results);
    const totalCredits = results.reduce((total, result) => total + result.credits, 0);
    const creditsEarned = getCreditsEarned(results);
    const semesterResult = results.every((result) => getResultStatus(getGradeFromMarks(result.marks, result.maxMarks)) === 'PASS') ? 'PASS' : 'FAIL';
    const currentHistory = [...previousSemesterHistory, { semester, sgpa: semesterSgpa, credits: totalCredits }];
    const cgpa = calculateCgpa(currentHistory);
    const history = [...previousSemesterHistory, { semester, sgpa: semesterSgpa, credits: totalCredits }].sort((first, second) => first.semester.localeCompare(second.semester));
    const summaryCards = [
        { title: 'Current CGPA', value: cgpa.toFixed(2), description: 'Credit-weighted average', icon: TrendingUp, accent: 'blue' as const },
        { title: 'Semester SGPA', value: semesterSgpa.toFixed(2), description: `${semester} performance`, icon: Calculator, accent: 'emerald' as const },
        { title: 'Credits Earned', value: `${creditsEarned} / ${totalCredits}`, description: 'Credits completed', icon: GraduationCap, accent: 'amber' as const },
        { title: 'Result', value: semesterResult, description: 'Semester result', icon: semesterResult === 'PASS' ? CheckCircle2 : XCircle, accent: semesterResult === 'PASS' ? 'rose' as const : 'rose' as const },
        { title: 'Class Rank', value: `${classRank.rank} / ${classRank.totalStudents}`, description: 'B.Tech CSE • Section A', icon: Trophy, accent: 'blue' as const },
    ];

    return (
        <main className="mx-auto max-w-[1500px] p-4 sm:p-6 lg:p-8">
            <section className="mb-6 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-indigo-600">Academic performance</p><h1 className="mt-2 text-2xl font-extrabold tracking-tight text-slate-950 sm:text-3xl">Results</h1><p className="mt-1.5 text-sm text-slate-500">B.Tech CSE <span className="px-1 text-slate-300">•</span> 3rd Year <span className="px-1 text-slate-300">•</span> {semester} <span className="px-1 text-slate-300">•</span> Section A</p></div><label className="relative block w-full sm:w-48"><span className="mb-1.5 block text-xs font-bold uppercase tracking-wider text-slate-400">Semester</span><select aria-label="Select semester" value={semester} onChange={(event) => setSemester(event.target.value as ResultSemester)} className="w-full rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm font-semibold text-slate-700 shadow-sm outline-none focus:border-indigo-500 focus:ring-2 focus:ring-indigo-100">{resultSemesterOptions.map((option) => <option key={option}>{option}</option>)}</select></label></section>
            <section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-5">{summaryCards.map((card) => <StatCard key={card.title} {...card} />)}</section>
            <section className="mt-6 rounded-2xl border border-slate-200/80 bg-white shadow-sm"><div className="flex items-center gap-3 border-b border-slate-100 px-5 py-4 sm:px-6"><div className="rounded-xl bg-indigo-50 p-2 text-indigo-600"><BookOpen className="h-5 w-5" /></div><div><h2 className="font-bold text-slate-900">Semester Results</h2><p className="text-xs text-slate-500">Subject-wise marks and grades for {semester}</p></div></div>{results.length > 0 ? <><div className="hidden overflow-x-auto md:block"><table className="w-full text-left"><thead className="border-b border-slate-100 text-[11px] font-bold uppercase tracking-wider text-slate-400"><tr><th className="px-6 py-3">Subject</th><th className="px-4 py-3">Code</th><th className="px-4 py-3">Credits</th><th className="px-4 py-3">Marks</th><th className="px-4 py-3">Grade</th><th className="px-4 py-3">Grade Point</th><th className="px-4 py-3">Status</th></tr></thead><tbody className="divide-y divide-slate-100">{results.map((result) => { const grade = getGradeFromMarks(result.marks, result.maxMarks); const status = getResultStatus(grade); return <tr key={result.subjectCode} className="text-sm"><td className="px-6 py-4 font-bold text-slate-800">{result.subjectName}</td><td className="px-4 py-4 font-semibold text-slate-500">{result.subjectCode}</td><td className="px-4 py-4 text-slate-600">{result.credits}</td><td className="px-4 py-4 font-semibold text-slate-700">{result.marks} / {result.maxMarks}</td><td className="px-4 py-4 font-extrabold text-slate-800">{grade}</td><td className="px-4 py-4 text-slate-600">{getResultGradePoint(result).toFixed(1)}</td><td className="px-4 py-4"><span className={`inline-flex items-center gap-1 rounded-full px-2.5 py-1 text-xs font-bold ${status === 'PASS' ? 'bg-emerald-50 text-emerald-700' : 'bg-rose-50 text-rose-700'}`}>{status === 'PASS' ? <CheckCircle2 className="h-3.5 w-3.5" /> : <XCircle className="h-3.5 w-3.5" />}{status}</span></td></tr>; })}</tbody></table></div><div className="grid gap-4 p-4 md:hidden">{results.map((result) => <ResultCard key={result.subjectCode} result={result} />)}</div></> : <div className="p-10 text-center"><p className="text-base font-bold text-slate-800">No results available</p><p className="mt-1 text-sm text-slate-500">Results for this semester have not been published yet.</p></div>}</section>
            <section className="mt-6 grid gap-6 xl:grid-cols-[0.9fr_1.1fr]"><div className="rounded-2xl border border-slate-200/80 bg-white p-5 shadow-sm sm:p-6"><div className="flex items-center gap-3"><div className="rounded-xl bg-emerald-50 p-2 text-emerald-600"><Award className="h-5 w-5" /></div><div><h2 className="font-bold text-slate-900">Semester Summary</h2><p className="text-xs text-slate-500">Calculated from subject results</p></div></div><div className="mt-5 grid grid-cols-2 gap-4"><div><p className="text-xs font-semibold text-slate-400">Total Credits</p><p className="mt-1 text-2xl font-extrabold text-slate-900">{totalCredits}</p></div><div><p className="text-xs font-semibold text-slate-400">Credits Earned</p><p className="mt-1 text-2xl font-extrabold text-slate-900">{creditsEarned}</p></div><div><p className="text-xs font-semibold text-slate-400">SGPA</p><p className="mt-1 text-2xl font-extrabold text-indigo-700">{semesterSgpa.toFixed(2)}</p></div><div><p className="text-xs font-semibold text-slate-400">Result</p><p className={`mt-1 text-lg font-extrabold ${semesterResult === 'PASS' ? 'text-emerald-700' : 'text-rose-700'}`}>{semesterResult}</p></div></div></div><div className="rounded-2xl border border-slate-200/80 bg-white p-5 shadow-sm sm:p-6"><div className="flex items-center gap-3"><div className="rounded-xl bg-indigo-50 p-2 text-indigo-600"><TrendingUp className="h-5 w-5" /></div><div><h2 className="font-bold text-slate-900">Academic Performance</h2><p className="text-xs text-slate-500">Semester-wise SGPA progression</p></div></div><div className="mt-4 divide-y divide-slate-100">{history.map((semesterResult) => <div key={semesterResult.semester} className="flex items-center justify-between gap-3 py-3"><div><p className="text-sm font-bold text-slate-800">{semesterResult.semester}</p><p className="mt-0.5 text-xs text-slate-500">{semesterResult.credits} credits</p></div><div className="text-right"><p className="text-sm font-extrabold text-indigo-700">SGPA: {semesterResult.sgpa.toFixed(2)}</p><span className="text-xs font-bold text-emerald-700">PASS</span></div></div>)}</div></div></section>
        </main>
    );
};